# Build results
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Publish output
dist/
publish/
run/
*.zip

# Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates
*.userprefs

# Build tooling
*.binlog
*.tlog
project.lock.json
project.assets.json
*.nuget.g.props
*.nuget.g.targets
*.nuget.dgspec.json
*.nuget.cache

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml
coverage*.json
coverage*.xml

# NuGet
*.nupkg
*.snupkg
packages/
.nuget/

# Windows / OS
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.stackdump

# Local runtime data: the SQLite database holds the router credentials, so it is never committed.
**/config/dyndns.db
**/config/dyndns.db-journal
**/config/dyndns.db-shm
**/config/dyndns.db-wal

# Logs
*.log

# Local design notes: the plans are scratched out while a change is being built. They are kept on disk
# for reference but are not part of the product or of the repository.
plans/
